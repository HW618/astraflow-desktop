// Shared ModelVerse endpoint definitions so the base URL is declared once
// instead of being duplicated across every ModelVerse client module.
export const MODELVERSE_BASE_URL = "https://api.modelverse.cn"
export const MODELVERSE_BASE_URL_V1 = "https://api.modelverse.cn/v1"
